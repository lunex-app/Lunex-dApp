# Screenshots

Drop product screenshots here so they render in the root `README.md` Preview section.

Expected filenames (PNG, ~1600px wide, dark theme):

| File            | Page                          |
| --------------- | ----------------------------- |
| `landing.png`   | Marketing landing (`/`)       |
| `swap.png`      | StableSwap (`/swap`)          |
| `bridge.png`    | CCTP Bridge & Gateway (`/bridge`) |
| `yield.png`     | Yield vaults (`/yield`)       |
| `analytics.png` | Analytics (`/analytics`)      |
| `wallet.png`    | Passwordless wallet connect modal |

The hero banner lives one level up at `docs/banner.png` (~2400×800px).
